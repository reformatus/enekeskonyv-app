import 'package:flutter/material.dart';

class VerseBar extends StatelessWidget {
  const VerseBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
      child: Text('Hello VerseBar!'),
    );
  }
}
